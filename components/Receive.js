import { Button, View, Text } from 'react-native';

const Receive = ({navigation}) => {
    return (
        <View>
            <Text>
                왜안돼
            </Text>
        </View>
    );
};

export default Receive;