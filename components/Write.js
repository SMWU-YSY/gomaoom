import { Button, View, Text } from 'react-native';

const Write = ({navigation}) => {
    return (
        <View>
            <Text>
                뭐여
            </Text>
        </View>
    );
};

export default Write;